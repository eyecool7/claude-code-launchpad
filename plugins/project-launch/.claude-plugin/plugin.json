{
  "name": "project-launch",
  "description": "바이브 코더를 위한 Claude Code Launchpad. 프로젝트 맞춤 Optimized PBS — 계획(Plan) · 구축(Build) · 보안(Shield). 가이드(/project-launch:guide) → 계획(/project-launch:plan) → 고도화(/project-launch:refine) → 셋업(/project-launch:setup) → 구축(/project-launch:build).",
  "author": {
    "name": "eyecool7"
  },
  "commands": "./commands/",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo ${CLAUDE_PLUGIN_ROOT} > /tmp/.project-launch-root"
          }
        ]
      }
    ]
  }
}